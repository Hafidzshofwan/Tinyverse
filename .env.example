# Phase 1 requires no environment variables.
# Cloud/BFF secrets (Firebase, AI, billing) are introduced in later phases
# and will live only in the deployment environment, never in the client bundle.
